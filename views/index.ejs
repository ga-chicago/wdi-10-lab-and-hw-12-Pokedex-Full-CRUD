<html>
<head>
	<title>Pokedex</title>
</head>
<body>
	<% for (let i = 0; i < pokemon.length; i++) { %>
		<a href="/pokemon/<%= i %>">
		<h1> <%= pokemon[i].name %> </h1> 
		<img src="<%= pokemon[i].img %>">
	<% } %>
</body>
</html>